import React from 'react';

const Navbar = () => (
  <nav style={{ padding: '1rem', backgroundColor: '#282c34', color: '#fff' }}>
    <h3>React + Jenkins + K8s</h3>
  </nav>
);

export default Navbar;
